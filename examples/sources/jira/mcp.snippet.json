{
  "_comment": "Merge each block into the matching committed MCP config: `claude` -> .mcp.json mcpServers, `cursor` -> .cursor/mcp.json mcpServers, `codex_toml` -> append to .codex/config.toml. The Atlassian remote server has NO read-only flag — read-only is enforced by the authenticated account's permissions (see SETUP.md).",
  "server_name": "jira",
  "read_only_mechanism": "credential-only",
  "claude": {
    "jira": { "type": "http", "url": "https://mcp.atlassian.com/v1/mcp" }
  },
  "cursor": {
    "jira": { "url": "https://mcp.atlassian.com/v1/mcp" }
  },
  "codex_toml": "[mcp_servers.jira]\nurl = \"https://mcp.atlassian.com/v1/mcp\"\nenabled = true"
}
